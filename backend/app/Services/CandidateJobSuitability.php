<?php
// salary, job level, job type, skills

use App\Models\Candidate;

class CandidateJobSuitability {

    // public function handle($candidate_id, $job_id, $type) {
    //     if($type == 0) {
    //         $candidate = Candidate::
    //         join('candidate_skill', 'candidates.id', '=', 'candidate_id')
    //         ->
    //     }
    // }
}